{
    "title": "Album",
    "description": "It's an album",
    "type": "object",
    "properties": {
        "title": {
            "description": "Album's title",
            "type": "string",
            "maxLength": 128,
            "faker": "random.word"
        },
        "description": {
            "description": "Album's description",
            "type": "string",
            "maxLength": 528,
            "faker": "random.words"
        },
        "image": {
            "description": "Album's cover image",
            "type": "string",
            "faker": "image.imageUrl"
        },
        "release date": {
            "description": "Albums's realease date",
            "type": "string",
            "faker": "date.past"
        },
        "id artist": {
            "description": "Artist id",
            "type": "integer",
            "faker": "random.number"
        },
        "id producer": {
            "description": "Producer's id",
            "type": "integer"
        },
        "id record label": {
            "description": "Record Label's id",
            "type": "integer"
        }
    },
    "required": ["title", "release date", "id artist"]
}