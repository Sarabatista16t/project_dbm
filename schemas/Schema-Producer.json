{
    "title": "Producer",
    "description": "It's the Producer",
    "type": "object",
    "properties": {
        "name": {
            "description": "Producer's name",
            "type": "string",
            "maxLength": 128,
            "faker": "name.findName"
        },
        "id company": {
            "description": "Producer's company",
            "type": "integer",
            "faker": "random.number"
        }
    },
    "required": [
        "name",
        "id company"
    ]
}