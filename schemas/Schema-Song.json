{
    "title": "Song",
    "description": "It's a song",
    "type": "object",
    "properties": {
        "title": {
            "description": "Songs's title",
            "type": "string",
            "maxLength": 128,
            "faker": "random.word"
        },
        "duration": {
            "description": "Song's durantion in seconds",
            "type": "integer",
            "minimum": 0,
            "maximum": 900,
            "faker": "random.number"
        },
        "id album": {
            "description": "Album's id",
            "type": "integer",
            "faker": "random.number"
        },
        "id genre": {
            "description": "Music genre's id",
            "type": "integer",
            "faker": "random.number"
        }
    },
    "required": [
        "title",
        "duration",
        "id album",
        "id genre"
    ]
}